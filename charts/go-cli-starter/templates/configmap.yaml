apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "go-cli-starter.configmapName" . }}
  labels:
    {{- include "go-cli-starter.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
